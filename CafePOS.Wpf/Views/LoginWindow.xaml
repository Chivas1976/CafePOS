<!-- CafePOS.Wpf/Views/LoginWindow.xaml -->
<Window x:Class="CafePOS.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Anmeldung" Height="220" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
      >
    <!--  Loaded="Window_Loaded"-->
    <!-- HIER -->

    <Grid Margin="16">
        <!-- Richtige Definitionen -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Loginname:" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <TextBox x:Name="LoginBox"
         Grid.Column="1"
         Text="{Binding Loginname, UpdateSourceTrigger=PropertyChanged}"
         MinWidth="200"/>

        <TextBlock Grid.Row="1" Text="Passwort:" VerticalAlignment="Center" Margin="0,8,8,0"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="PwBox"
                 Margin="0,8,0,0" MinWidth="200"
                 PasswordChanged="PwBox_PasswordChanged"/>

        <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
               Text="{Binding Fehlermeldung}" Foreground="Tomato"
               Margin="0,8,0,0"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2"
                Orientation="Horizontal" HorizontalAlignment="Right"
                Margin="0,12,0,0">
            <Button Content="Abbrechen" Width="100" Margin="0,0,8,0"
              Command="{Binding AbbrechenCmd}"/>
            <Button Content="Anmelden" Width="100" IsDefault="True"
              Command="{Binding AnmeldenCmd}" />
        </StackPanel>
    </Grid>
</Window>
